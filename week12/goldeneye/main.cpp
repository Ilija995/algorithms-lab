#include <iostream>
#include <vector>

void testcase()
{
    int n_jammers, n_missions, power;
    std::cin >> n_jammers >> n_missions >> power
}

int main()
{
    std::ios_base::sync_with_stdio(false);
    int T; std::cin >> T; while (T--) testcase();
    return 0;
}
